name: Deploy Django App

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy to AWS EC2
    runs-on: ubuntu-latest

    environment: portfolio  # the environment name on GitHub

    steps:
    - name: Checkout Repository
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'

    - name: Install Dependencies
      working-directory: ./portfolio_backend  # Correct path to portfolio_backend
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run Tests
      working-directory: . # Correct path to portfolio_backend
      run: python manage.py test

    - name: Configure AWS Credentials
      uses: aws-actions/configure-aws-credentials@v3
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: eu-central-1

    - name: Install Terraform
      uses: hashicorp/setup-terraform@v2
      with:
        terraform_version: 1.4.6

    - name: Terraform Init
      working-directory: ./terraform  # Correct path to Terraform config
      run: terraform init

    - name: Terraform Plan
      working-directory: ./terraform  # Correct path to Terraform config
      run: terraform plan

    - name: Terraform Apply
      working-directory: ./terraform  # Correct path to Terraform config
      run: terraform apply -auto-approve

    - name: Fetch EC2 Public IP
      id: output
      working-directory: ./portfolio_backend/terraform  # Correct path to Terraform config
      run: echo "EC2_IP=$(terraform output -raw ec2_public_ip)" >> $GITHUB_ENV

    - name: Post-deploy Verification
      run: |
        echo "Deployment Completed."
        echo "Application is available at http://${{ env.EC2_IP }}/"
